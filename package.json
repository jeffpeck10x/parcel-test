{
  "name": "@parcel-test/test",
  "version": "0.0.0",
  "private": true,
  "description": "Parcel Test",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @parcel-test/demo build",
    "dev": "yarn workspace @parcel-test/demo dev",
    "serve-static-demo": "cd packages/demo/dist && serve"
  },
  "devDependencies": {
    "@deck.gl/core": "^8.5.5",
    "@loaders.gl/core": "^3.0.0",
    "parcel": "^2.0.0-beta.3.1",
    "serve": "^12.0.0"
  }
}
